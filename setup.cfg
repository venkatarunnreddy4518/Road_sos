[flake8]
max-line-length = 100
exclude =
    .git,
    .venv,
    build,
    ios,
    android,
    backend/.venv
ignore = E203, W503

[pylint]
max-line-length = 100

[pyupgrade]
py311-plus = true

[tool:pytest]
testpaths = backend/tests
